
services:
  database:
    image: postgres:15-alpine
    container_name: postgres-perfumes
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT}:5432"
    volumes:
      - db_perfumes_data:/var/lib/postgresql/data

volumes:
  db_perfumes_data: